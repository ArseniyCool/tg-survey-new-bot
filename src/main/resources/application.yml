# CODEX: Do not commit secrets. Provide TELEGRAM_TOKEN (and DB_* if needed) via environment/secret storage.
telegram:
  token: ${TELEGRAM_TOKEN}

micronaut:
  server:
    enabled: false # CODEX: set true only if you use webhooks/API

    port: ${MICRONAUT_SERVER_PORT:800} # CODEX: avoid 8080 conflicts
#micronaut:
#  server:
#    port: 8080

# CODEX: Datasource config moved to application-db.yml (enable with MICRONAUT_ENVIRONMENTS=db).
