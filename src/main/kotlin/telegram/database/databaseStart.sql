CREATE TABLE users (
                       id BIGINT PRIMARY KEY,
                       state VARCHAR(50),
                       phone VARCHAR(20),
                       project_name VARCHAR(255)
);